@model IEnumerable<Pokedex.Models.FoundAt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Map.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PKMN.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Map.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PKMN.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FoundAtID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FoundAtID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FoundAtID })
        </td>
    </tr>
}

</table>
