@model IEnumerable<Pokedex.Models.Caught>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PKMN.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PKMN.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CaughtID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CaughtID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CaughtID })
        </td>
    </tr>
}

</table>
